/* eslint-disable @typescript-eslint/no-unused-vars */

/**
 * Unions type allows you to set multiple types as a type
 * using `|` as a separator.
 */
let score: number | string = 13;
score = '22';
score = 3;

export {};
