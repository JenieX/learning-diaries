/**
 * Array.prototype.splice()
 *
 * The splice() method changes the contents of an array by removing or replacing
 * existing items and/or adding new items in place.
 *
 * Syntax:
 * splice(start)
 * splice(start, deleteCount)
 * splice(start, deleteCount, itemToAdd)
 * splice(start, deleteCount, itemToAdd, secondItemToAdd)
 *
 * Return value:
 * An array containing the deleted items.
 *
 * MDN/Web/JavaScript/Reference/Global_Objects/Array/splice
 */
